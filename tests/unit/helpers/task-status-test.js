
import { taskStatus } from 'gmtd5-ember/helpers/task-status';
import { module, test } from 'qunit';

module('Unit | Helper | task status');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = taskStatus([42]);
  assert.ok(result);
});

